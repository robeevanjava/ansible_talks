# -*- mode: ruby -*-
# vi: set ft=ruby :

Vagrant.configure(2) do |config|
  config.vm.box = "ubuntu_trusty64"

  # config.vm.provision "ansible" do |ansible|
  #   ansible.host_key_checking = false
  #   ansible.playbook = "filters_demo_1/playbook.yml"
  #   ansible.limit = 'all'
  #   ansible.verbose = 'v'
  # end
end
